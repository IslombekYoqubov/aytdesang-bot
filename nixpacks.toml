[phases.setup]
nixPkgs = ['python311Full', 'python311Packages.pip', 'python311Packages.setuptools']

[phases.install]
cmds = ['python3.11 -m ensurepip --upgrade', 'python3.11 -m pip install -r requirements.txt']

[start]
cmd = 'python3.11 main.py'
